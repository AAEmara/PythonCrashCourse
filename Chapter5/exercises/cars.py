# Assigning a list of variables.
cars = ['audi', 'bmw', 'subaru', 'toyota']
# Using a FOR loop to check for equalities using IF statements. 
for car in cars:
    if car == "bmw":
        print(car.upper())
    else:
        print(car.title())
